import User from '../models/User';

export default class UserWorker {
    /**
     * register a user in the system
     * @param {object} userDetails 
     */
    registerUser = (userDetails: any) => userDetails;
}

